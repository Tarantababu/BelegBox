# Requirement R-2: every stored verdict names the exact versions that produced
# it, so a 2026 verdict can be re-derived in 2033. Bumping any value here is a
# deliberate, reviewed event - the corpus snapshots will show what moved.
#
# UNVERIFIED. These are placeholders. F1 week 1 pins each to a release that has
# actually been built and resolved, and records the configuration SHA-256.
validator.config.version=UNPINNED
validator.config.sha256=UNPINNED
kosit.validationtool.version=UNPINNED
mustang.library.version=UNPINNED
