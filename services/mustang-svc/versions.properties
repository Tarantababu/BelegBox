# Requirement R-2: every stored verdict names the exact versions that produced
# it, so a 2026 verdict can be re-derived in 2033. Bumping any value here is a
# deliberate, reviewed event - the corpus snapshots will show what moved.
#
# The configuration digest is verified by scripts/fetch-validator-config.sh on
# every fetch. A release replaced in place fails the check rather than silently
# changing what "valid" means.
validator.config.version=v2026-01-31
validator.config.sha256=6a5a5911a421b25fbc423f62f93f894df7b236f5d73ca4f84bb222a945082704
kosit.validationtool.version=1.5.0
